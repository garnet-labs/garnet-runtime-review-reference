# Vendored packages

Local registry stand-ins used by the testbed app.

- `chart-helpers-1.0.0.tgz` — installable artifact referenced from
  `npm-testbed/package.json` via a `file:` specifier.

Package sources, the build script, and full fixture documentation live on the
[`fixture-sources`](../../../../tree/fixture-sources/npm-testbed/vendor) branch.
